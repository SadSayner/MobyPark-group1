name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13' 

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest httpx

      - name: Run Unit Tests
        run: |
          # Draai lokale tests (zonder Docker)
          pytest v1/tests 

  e2e-tests:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Start services
        run: docker compose up -d
        
      - name: Run E2E tests
        run: |
          # Wachten op Elasticsearch
          sleep 30 
          
          # Forceer installatie van test-tools in de container
          docker compose exec -T api pip install pytest httpx
          
          # Draai tests met SKIP_SEED om de 2 uur wachttijd te voorkomen
          docker compose exec -T -e MOBYPARK_SKIP_SEED=1 api python -m pytest e2e/

  build-and-push:
    needs: [test, e2e-tests]
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/mobypark-api:latest